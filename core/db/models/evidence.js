'use strict';
const { randomUUID }=require('crypto'); const db=()=>require('../database').get(); const eventBus=require('../../events/event-bus');
function findById(id){return db().prepare('SELECT * FROM evidence WHERE id=?').get(id)??null;} function findByIdForEngagement(id,eid){return db().prepare('SELECT * FROM evidence WHERE id=? AND engagement_id=?').get(id,eid)??null;} function findByEngagement(eid){return db().prepare('SELECT * FROM evidence WHERE engagement_id=? ORDER BY created_at DESC').all(eid);} function findByTarget(tid){return db().prepare('SELECT * FROM evidence WHERE target_id=? ORDER BY created_at DESC').all(tid);} function findByTargetForEngagement(tid,eid){return db().prepare('SELECT * FROM evidence WHERE target_id=? AND engagement_id=? ORDER BY created_at DESC').all(tid,eid);}
function create({engagementId,type,module:mod,targetId,label,data,path:p}){const id=randomUUID(); db().prepare('INSERT INTO evidence (id,engagement_id,type,module,target_id,label,data,path) VALUES (?,?,?,?,?,?,?,?)').run(id,engagementId,type,mod,targetId??null,label??null,data??null,p??null); eventBus.emit('evidence:created',{engagementId,subject:id,evidenceId:id,type,module:mod,targetId,label,path:p}); return id;}
function removeForEngagement(id,eid){const r=db().prepare('DELETE FROM evidence WHERE id=? AND engagement_id=?').run(id,eid); if(r.changes)eventBus.emit('evidence:deleted',{engagementId:eid,subject:id,evidenceId:id}); return r;} function remove(id){const row=findById(id); return removeForEngagement(id,row?.engagement_id);} module.exports={findById,findByIdForEngagement,findByEngagement,findByTarget,findByTargetForEngagement,create,remove,removeForEngagement};